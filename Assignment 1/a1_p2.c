/*
  CH-230-A
  a1_p2.c
  Flavia Tasellari
  ftasellari@jacobs-university.de
*/
 
#include <stdio.h>
 
int main () {
    int result; /* The result of our calculation */
    result = (2 + 7) * 9 / 3;
    printf (" The result is %d\n", result); 
    /* Here the word "result" is missing that's why random values are printed
       When I write "result", the correct result of the calculation is printed. */

    return 0;
}